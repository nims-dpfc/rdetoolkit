from .core import resize_image_aspect_ratio

def resize_image_aspect_ratio(input_path: str, output_path: str, width: int, height: int) -> None:
    """Resize the image while maintaining the aspect ratio.

    Args:
        input_path (str): The path of the image file to be resized.
        output_path (str): The path of the resized image file.
        width (int): The width of the resized image.
        height (int): The height of the resized image.

    """
    ...
